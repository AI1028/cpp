#include <iostream>

using namespace std;

int main()
{   
    string line;
    string word;
    while (getline(cin, line))
        cout << line << endl;
    while (cin >> word)
        cout << word << endl;

    return 0;
}
